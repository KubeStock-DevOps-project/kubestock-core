# Test Runner Service - Environment Configuration

# ============================================================================
# Service Configuration
# ============================================================================
PORT=3007
HOST=0.0.0.0

# ============================================================================
# Asgardeo Authentication (Required)
# ============================================================================

# Token endpoint URL
ASGARDEO_TOKEN_URL=https://api.asgardeo.io/t/yourorg/oauth2/token

# Client credentials (Required for all auth types)
ASGARDEO_CLIENT_ID=your_client_id_here
ASGARDEO_CLIENT_SECRET=your_client_secret_here

# ============================================================================
# User Authentication (Optional - for default user login)
# ============================================================================

# If these are set, tests will use user authentication by default
# Otherwise, M2M (client credentials) will be used
# You can override these per test via the API request body

# ASGARDEO_USERNAME=testuser@example.com
# ASGARDEO_PASSWORD=SecurePassword123

# ============================================================================
# Gateway Configuration (Optional)
# ============================================================================

# Default gateway URL for smoke tests
# Can be overridden per test via API request
GATEWAY_URL=http://localhost:5173

# For Docker/Kubernetes environments:
# GATEWAY_URL=http://host.docker.internal:5173
# GATEWAY_URL=http://gateway-service:5173

# ============================================================================
# Example Configurations for Different Environments
# ============================================================================

# --- Local Development ---
# GATEWAY_URL=http://localhost:5173

# --- Docker Compose ---
# GATEWAY_URL=http://host.docker.internal:5173

# --- Kubernetes ---
# GATEWAY_URL=http://gateway-service.default.svc.cluster.local:5173

# ============================================================================
# Notes
# ============================================================================

# 1. Never commit real credentials to version control
# 2. Use .env.example for templates
# 3. Authentication priority:
#    - Request body auth params (highest)
#    - Environment variables (ASGARDEO_USERNAME/PASSWORD)
#    - M2M client credentials (fallback)
# 4. All configuration can be overridden via API request body
