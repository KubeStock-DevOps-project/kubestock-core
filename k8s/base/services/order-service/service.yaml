apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: inventory-system
  labels:
    app: order-service
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: order-service
