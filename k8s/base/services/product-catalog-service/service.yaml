apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  namespace: inventory-system
  labels:
    app: product-catalog-service
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: product-catalog-service
