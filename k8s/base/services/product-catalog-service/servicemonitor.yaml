apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: product-catalog-service-monitor
  namespace: inventory-system
  labels:
    app: product-catalog-service
    prometheus: inventory
spec:
  selector:
    matchLabels:
      app: product-catalog-service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
