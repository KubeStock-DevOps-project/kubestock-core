apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: inventory-system
  labels:
    app: user-service
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: user-service
