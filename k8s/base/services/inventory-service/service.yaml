apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: inventory-system
  labels:
    app: inventory-service
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: inventory-service
