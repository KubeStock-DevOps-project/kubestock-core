apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: supplier-service-monitor
  namespace: inventory-system
  labels:
    app: supplier-service
    prometheus: inventory
spec:
  selector:
    matchLabels:
      app: supplier-service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
